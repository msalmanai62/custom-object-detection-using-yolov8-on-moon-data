path: /content/drive/MyDrive/moon detection/monn_data
train: data/images
val: data/images
# test: dataimages

# Number of classes
nc: 1
# Class names
names: ['moon']
# Optional: Batch size, epochs, etc. (these can also be set in the training script)
batch: 8
epochs: 5
# img_size: 640  # Image size to use for training (height and width)

roboflow:
  workspace: msai62-annotations
  project: moon-annotations
  version: 1
  license: CC BY 4.0
  url: https://universe.roboflow.com/msai62-annotations/moon-annotations/dataset/1 